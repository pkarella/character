$(document).ready(
  function() {
    $("#info-form").submit(function(event) {
      event.preventDefault();
      var range = $("#range").val();
      var magic = $("#magic").val();
      var jewelry = $("#jewelry").val();

      if (
      };

    });
  }
);
